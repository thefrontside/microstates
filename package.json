{
  "name": "microstates-monorepo",
  "version": "0.0.0-monorepo",
  "description": "Composable State Primitives for JavaScript",
  "author": "Frontside Engineering <engineering@frontside.io>",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint",
    "coverage": "yarn workspaces run coverage",
    "coveralls": "yarn workspaces run coveralls"
  }
}
