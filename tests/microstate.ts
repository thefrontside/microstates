import 'jest';

describe('microstates', () => {
  describe('transitions', () => {
    class Person {
      details = Object;
      age = Number;
      pets = Array;
    }
    class Todo {
      title = String;
      isCompleted = Boolean;
      owner = Person;
    }
  });
});
